{
  "name": "mandarin-tutor-app",
  "version": "1.0.0",
  "private": true,
  "description": "Mandarin Tutor — native Android wrapper (Capacitor)",
  "scripts": {
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
